require "spec_helper"

describe ImapClear do
  it "has a version number" do
    expect(ImapClear::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
